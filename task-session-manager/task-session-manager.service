[Unit]
Description=33GOD Session Manager Service
Documentation=https://github.com/delorenj/flume
After=network.target rabbitmq-server.service
Wants=rabbitmq-server.service

[Service]
Type=simple
User=taskmanager
Group=taskmanager
WorkingDirectory=/opt/task-session-manager

# Environment variables
Environment="RABBITMQ_URL=amqp://guest:guest@localhost:5672/"
Environment="RABBITMQ_EXCHANGE=amq.topic"
Environment="RABBITMQ_QUEUE=task.lifecycle"
Environment="RABBITMQ_ROUTING_KEY=task.lifecycle.*"
Environment="FLUME_SESSION_MANAGER=zellij"
Environment="LOG_LEVEL=info"
Environment="ENVIRONMENT=production"
Environment="HEALTH_CHECK_PORT=9344"

# Additional environment from file (optional)
EnvironmentFile=-/etc/task-session-manager/env

# Main process
ExecStart=/usr/local/bin/task-session-manager

# Restart policy
Restart=unless-stopped
RestartSec=5s

# Resource limits
LimitNOFILE=65536
LimitNPROC=4096

# Security hardening
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/tmp /var/log/task-session-manager

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=task-session-manager

# Graceful shutdown
TimeoutStopSec=30
KillMode=mixed
KillSignal=SIGTERM

[Install]
WantedBy=multi-user.target
