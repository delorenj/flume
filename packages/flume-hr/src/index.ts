#!/usr/bin/env node
/**
 * The `flume` CLI.
 *
 * Scaffold only. The implementation arrives with the code moved out of pjangler:
 * `src/org/` (the org chart and reviews), `src/hire/` (the onboarding chain) and
 * the employee parity rules.
 */

import { describeStanding, rollUpStanding } from "@delorenj/flume-core";

export { describeStanding, rollUpStanding };

console.error("flume: not wired up yet -- the workforce is still being moved in.");
process.exit(70);
